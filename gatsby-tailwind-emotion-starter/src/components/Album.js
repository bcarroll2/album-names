import React from "react"
import { useStaticQuery, graphql } from "gatsby"

const Album = (props) => {

  // const data = useStaticQuery(graphql`
  //   query AlbumQuery($skip: Int) {
  //     allTwitterStatusesUserTimelineAlbumNames(skip: $skip) {
  //       edges {
  //         node {
  //           full_text
  //         }
  //       }
  //     }
  //   }
  // `)
  // return (
  //   <div>{JSON.stringify(data.allTwitterStatusesUserTimelineAlbumNames.edges[props.index].node.full_text, null, 4)}</div>
  // )
}


export default Album

